# Local backend: http://localhost:8000
# Shared backend: ask the backend team for its current URL.
VITE_API_BASE=http://localhost:8000
# Optional P4 handoff after a before/after comparison completes.
VITE_SIMULATOR_URL=http://localhost:5174
