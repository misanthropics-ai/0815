# Local backend: http://localhost:8000
VITE_API_BASE=http://localhost:8000

# Optional navigation back to the P5 Diagnosis + Debate frontend.
VITE_DIAGNOSIS_URL=http://localhost:5173

# Optional: mock | live. Omit to let the backend choose automatically.
# VITE_API_MODE=live

# Demo insurance: cached responses are deterministic and fast.
VITE_USE_CACHE=true
